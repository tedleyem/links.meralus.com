# Jekyll
remote_theme: digitalmalayali/linkhub-jekyll-theme

# Site Configuration
name: Meralus.com  # Name of your website
tagline: Tedley M  # Set your preferred page title
description: Father, Software Engineer, Twin ✌🏿 # Also used as a meta description
favicon: assets/favicon/favico-io-ted-cartoon-trans/favicon.ico # Path / URL to the favicon of your website (e.g., 'assets/images/favicon.png')
logo: assets/images/techdadteddy-pixel.png # Path / URL to the logo (e.g., 'assets/images/logo.png')
url: https://meralus.coms # URL of your website
locale: en_GB # The value specifies the locale in which these tags are marked up. It follows the format `language_TERRITORY`.
ga4:  # Google Analytics 4 measurement ID (optional). Remove it if not needed.
twitter:
  username: techdadteddy # Used for SEO
image:
  path: assets/images/icons/twitter.svg # Used for SEO
  type: jpeg # File format of image. Change according to your image. Supported types: https://en.wikipedia.org/wiki/Media_type#Common_examples
  height: 100 # Pixels
  width: 100
  alt: logo

# Verified Badge Configuration
#badge: true  # Set to 'true' or 'false' to enable / disable the badge
#icon: ri:verified-badge-fill  # Enter the icon name for the badge from Iconify Design (https://icon-sets.iconify.design/)
#color: '#dfb221'  # Define the color of the badge
 
# Defaults
defaults:
  -
    scope:
      path: ""
    values:
      layout: "default"    

# Exclude
exclude: [README.md, Gemfile.lock, .jekyll-cache/, .github/, CHANGELOG.md, Gemfile, LICENSE.txt, funding.yml, linkhub-jekyll-theme.gemspec]